version: '3.8'

services:
  web:
    build: .
    ports:
      - "${APP_PORT}:${APP_PORT}"
    environment:
      - FLASK_DEBUG=1
      - FLASK_APP=app.py
    volumes:
      - .:/app
    command: sh -c "flask run --host=0.0.0.0 --port=${APP_PORT}"